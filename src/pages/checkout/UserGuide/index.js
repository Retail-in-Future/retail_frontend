import React from 'react';
import Step from './Step';

export default () => (
    <div>
        <Step step={1} instructions="举起双手" />
        <Step step={1} instructions="举起双手" />
        <Step step={1} instructions="举起双手" />
        <Step step={1} instructions="举起双手" />
    </div>
);
